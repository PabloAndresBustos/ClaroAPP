export interface Images{
    title: string,
    url: string,
    buttons: string,
    class: string,
    modalSubTitle?: string,
    modalOpen?: string,
    modalContent?: string[],
    modalIcon: boolean
}